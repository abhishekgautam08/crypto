import { createTheme } from "@mui/material";


const theme = createTheme({
  palette: {
    mode: "dark", // Set the dark mode
    primary: {
      main: "#14161a", // Set your primary color
    },
    text: {
      primary: "white", // Set your text color
    },
  },
});

export default theme;
